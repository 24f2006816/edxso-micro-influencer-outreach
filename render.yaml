services:
  - type: web
    name: edxso-micro-influencer-outreach
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    healthCheckPath: /
